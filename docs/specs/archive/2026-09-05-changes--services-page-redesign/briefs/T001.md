# Brief T001 — Extraer el catálogo a datos con orden explícito

Eres el **Implementer** de una tarea AKILI. Ejecuta **solo T001**. Un Reviewer independiente auditará tu diff.

## Lee primero, en este orden

1. `docs/specs/changes/services-page-redesign/tasks.md` → sección `## T001`
2. `docs/specs/changes/services-page-redesign/design.md` → §3 Data Model, y las decisiones **DD-020** y **DD-027**
3. `client/src/app/features/services/services-page/services-page.ts`

## Alcance

**Solo** `client/src/app/features/services/services-page/`.

- **No** toques CSS, HTML, plantillas, ni los diccionarios `es.json` / `en.json`.
- **No** empieces T002…T007.
- **No** hagas commit ni cambies de rama.

## Qué construir

Crear `service-catalog.data.ts` en esa carpeta, moviendo desde `services-page.ts`:

- Las interfaces `ServiceSub` y `ServiceGroup`.
- `SERVICE_GROUP_IDS` — exactamente los mismos 5 ids, en el mismo orden: `contabilidad`, `administrativa`, `riesgo`, `asesoria`, `marca`.
- Nueva constante `SERVICE_VISIBLE_LIMIT = 6`.
- `SERVICE_GROUPS`: los 5 grupos, con `subs` como **arrays explícitos** de pares `{ titleKey, descKey }`.

Eliminar la función generadora `subKeys()`. Ya no sirve: produce claves correlativas (`subC01t`…`subC16t`) y el orden aprobado **no** es correlativo.

Reordenar **solo** el grupo `contabilidad` a este orden comercial (las claves no cambian, cambia su posición en el array):

1. `subC01` Estados Financieros
2. `subC06` Declaraciones
3. `subC08` Revisoría Fiscal / Auditoría
4. `subC09` Facturación electrónica
5. `subC07` Información exógena
6. `subC05` Asesoría contable / tesorería
7. `subC02` Sistema contable
8. `subC03` Inventarios
9. `subC04` Costos
10. `subC10` Actualización RUB
11. `subC11` Cámara de Comercio
12. `subC12` RUT
13. `subC13` RUP para ESAL
14. `subC14` Proyecciones financieras
15. `subC15` Certificado de ingresos
16. `subC16` RNT y TRA

Los otros 4 grupos conservan su orden actual.

En `services-page.ts`: importar desde el nuevo archivo y **re-exportar** `SERVICE_GROUP_IDS` desde ahí, para que `contact-section.ts` y `contact-intent.model.ts` sigan funcionando **sin tocarlos**.

## Reglas duras

- **DD-027 — no abras los diccionarios.** El reorden se hace moviendo entradas del array. Mover valores entre claves de `es.json`/`en.json` remapearía 16 pares título↔descripción y **ningún test lo detectaría**.
- Los 16 pares `titleKey`/`descKey` de Contabilidad deben seguir emparejados exactamente como están hoy (`subC06t` va con `subC06d`, nunca con otro).
- **No relajes ningún test.** Si el conteo de 16 o de 4 falla, arregla el código, nunca la aserción. Ese conteo es el único gate real de otro requisito.
- No añadas dependencias a `package.json`.

## Verificación obligatoria antes de reportar

```bash
cd client && npm run test:agent -- services-page contact-section
```

Si falla, arréglalo y vuelve a correrlo. No reportes con la suite en rojo.

## Formato de reporte

Cuando termines, responde en una sola línea:

```
T001 DONE | archivos: <lista> | test: PASS|FAIL | notas: <una línea>
```
