# T009 — reporte

- Estado: DONE
- Archivos: client/src/app/features/services/services-page/services-page.css
- Tests: PASS (235) · lint limpio · build sin avisos de presupuesto
- Causa del desbordamiento residual: La técnica de full-bleed (`margin: 0 calc(50% - 50vw); padding: 0.6rem calc(50vw - 50% + 0.25rem);`) aplicada a `.rail` dentro de un grid item en `.catalog.wrap`. En un grid item cuyo bloque contenedor ya tiene padding lateral (`var(--pad-x)` = 20px), la disparidad entre `50vw` y `50%` combinada con el modelo de caja de Blink/WebKit para márgenes negativos + padding lateral en grid generaba una expansión asimétrica hacia la derecha de ~86px más allá del viewport.
- Cambios aplicados:
  - En `@media (max-width: 1099px)`:
    - `.catalog { grid-template-columns: minmax(0, 1fr); }`: permite que la columna de CSS Grid reduzca su ancho por debajo del `min-content` (748px) de la lista de chips.
    - `.rail`: se reemplazó el margen negativo/padding full-bleed por contención dentro de `.wrap` (`margin: 0 0 2rem; padding: 0.6rem 0; min-width: 0;`), eliminando completamente la dependencia de `50vw`.
    - `.rail__list`: añadido `min-width: 0;` y `-webkit-overflow-scrolling: touch;`, conservando el scroll horizontal interno de los chips (`overflow-x: auto; scrollbar-width: none;`).
    - `.chapters`: añadido `min-width: 0;` para asegurar que el contenedor de capítulos no fuerce ancho mínimo.
  - En `@media (max-width: 720px)`:
    - `.subs { grid-template-columns: minmax(0, 1fr); }`: asegura que la grilla de sub-servicios en mobile colapse correctamente a columna única sin desbordamiento.
- Riesgo para escritorio: Nulo. Todas las modificaciones se realizaron dentro de bloques `@media (max-width: 1099px)` y `@media (max-width: 720px)`. Las reglas de escritorio (≥1100px) permanecen 100% intactas (`grid-template-columns: 248px 1fr`, rail vertical con `position: sticky`, y grilla `.subs` a 2 columnas).
- Notas: Cumplimiento estricto de KZ-002: no se tocó `html` ni `body`, ni se usó `overflow-x: hidden` superficial; solo se modificó `services-page.css` sin alterar templates, TypeScript ni pruebas.
