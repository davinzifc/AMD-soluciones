# T005 — reporte

- Estado: DONE
- Archivos: client/src/app/features/services/services-page/services-page.ts, client/src/app/features/services/services-page/services-page.spec.ts
- Tests: PASS (223 tests en suite completa, 19 en services-page.spec.ts)
- Guarda de lista vacía: En ServicesPage.checkActiveChapter() con `if (chapterElements.length === 0) return;` que sale antes de invocar resolveActiveChapter y sin modificar la señal; verificado en spec mockeando querySelectorAll para retornar un NodeList vacío.
- Limpieza al destruir: En destroyScrollSpy(), se invoca cancelAnimationFrame si rafId !== null y se ejecuta scrollCleanup() para llamar window.removeEventListener('scroll') y window.removeEventListener('resize'); verificado en spec con espías a window.removeEventListener y window.cancelAnimationFrame.
- Notas: Cableado de scroll-spy implementado con afterNextRender, listeners fuera de la zona de Angular coalescidos a rAF (NFR-001), re-entrada a la zona solo en cambio de id y sin IntersectionObserver (DD-017).
