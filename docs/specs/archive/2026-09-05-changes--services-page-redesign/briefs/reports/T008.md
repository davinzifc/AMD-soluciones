# T008 — reporte

- Estado: DONE
- Archivos: client/src/assets/i18n/es.json, client/src/assets/i18n/en.json, client/src/app/features/services/services-page/services-page.ts, client/src/app/features/services/services-page/services-page.html, client/src/app/features/services/services-page/services-page.spec.ts, client/src/app/app.config.ts
- Tests: PASS (234 tests en suite completa, 20 en services-page.spec.ts)
- Offset aplicado y cómo se adapta al breakpoint: Se configuró una función dinámica en `ViewportScroller.setOffset(() => [0, yOffset])` provista mediante `provideAppInitializer` en `app.config.ts`. La función evalúa `window.innerWidth <= 1099`: en móvil/tablet calcula `navH + 4.5 * rootFontSize` (144px con rem base de 16px) para no quedar tapado por el top-nav sticky (72px) ni por el rail de chips sticky (4.5rem adicionales); en escritorio (> 1099px) calcula `navH + 1.5 * rootFontSize` (96px). Esto empareja la física de scroll nativo de `scroll-margin-top` del CSS y se adapta tanto al breakpoint como a la escala de fuente del usuario.
- Prueba de falsabilidad del gate de fidelidad: Se eliminó temporalmente el elemento `<h2>{{ 'svcCloserTitle' | localize }}</h2>` dentro de `.closer__panel` en `services-page.html` y se ejecutó `npm run test:agent`. El test falló unívocamente con `AssertionError: expected null to be truthy` en `services-page.spec.ts:339` al verificar `expect(h2).toBeTruthy()`. Tras confirmar el fallo, se restauró el `<h2>` y la suite completa retornó a 234 tests en verde.
- Notas: Fidelidad de hero y closer restaurada 1:1 con el mockup, conteos derivados dinámicamente del catálogo de servicios, `backRoad` sustituido por WhatsApp con URL calculada sin literales, y gate de fidelidad documentado con qué no prueba.
