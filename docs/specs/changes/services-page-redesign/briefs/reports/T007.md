# T007 — reporte

- Estado: DONE
- Archivos: client/src/app/features/home/contact/contact-section.ts, client/src/app/features/home/contact/contact-section.spec.ts
- Tests: PASS (232 tests en suite completa, 17 en contact-section.spec.ts)
- Skills cargadas:
  - `angular-developer`: Inyección de `ActivatedRoute` con `{ optional: true }` y lectura sincrónica de `snapshot.queryParamMap` en inicializadores de clase para el `FormGroup` inicial de Reactive Forms; uso de `effect()` en el constructor para suscribirse reactivamente al diccionario de `LocaleService` y actualizar el campo de mensaje al cambiar de idioma sólo si el control sigue `pristine`.
  - `error-handling-patterns`: Validación defensiva con la guarda `isServiceGroupId` y manejo seguro de parámetros de consulta para garantizar degradación elegante ante valores inválidos (`servicio=noexiste`) o ausentes sin lanzar excepciones ni corromper el formulario.
- Decisión de implementación: Se utilizó `inject(ActivatedRoute, { optional: true }).snapshot.queryParamMap` para leer los parámetros una sola vez al inicializar según la regla 1 de §4 del diseño (sin suscripciones a streams ni mutaciones externas), junto con un `effect()` acoplado al signal del diccionario de `LocaleService` para actualizar la clave `detalle` en caliente ante cambios de idioma (REQ-005, KZ-003) respetando las modificaciones del usuario si el campo ya fue editado.
- Notas: Preselección de servicio e interpolación de detalle 100% en cliente sin llamadas a API (fase 1 / ADR-003) ni analítica adicional; cada aserción de presencia en el spec incluye su comentario explicando qué no prueba.
