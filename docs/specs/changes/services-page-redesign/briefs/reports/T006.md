# T006 — reporte

- Estado: DONE
- Archivos: client/src/app/features/services/services-page/services-page.css
- Tests: PASS (223 tests)
- Dónde quedó `scroll-behavior` y por qué: En client/src/styles.css sobre html (ya configurado globalmente en el repositorio con regla prefers-reduced-motion: reduce), dado que scroll-behavior en la raíz del documento no puede aplicarse desde :host sin violar el encapsulamiento de estilos del componente; en services-page.css se definió scroll-margin-top en .chapter (calc(var(--nav-h) + 1.5rem) en desktop y calc(var(--nav-h) + 4.5rem) en móvil bajo el chip rail).
- Selectores con ancestro body/html: ninguno
- Tokens declarados en :host: --ink-text, --ink-text-2, --ink-text-3, --hairline, --nav-h
- Notas: CSS portado fielmente del mockup v0.1.1 con scope de componente en :host, colapso vía data-collapsed/data-overflow, objetivos táctiles >=44px, foco visible dorado y cero colores fuera de tokens AMD.
